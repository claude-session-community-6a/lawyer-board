# The image is packaged from build output only, so this is an allowlist:
# exclude everything, then admit exactly what the Dockerfile copies. Sources,
# .git and env files never enter the context.
*
!dist
!node_modules
!package.json
